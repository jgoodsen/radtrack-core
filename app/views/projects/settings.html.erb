
<div id="project_settings_tabs" style="float: left; width: 100%;">
	
     <ul>
         <li><a href="#process"><span>Process</span></a></li>
       <li><a href="#members"><span>Team Members</span></a></li>
     </ul>

    <div id="members">
  		<%= render :partial => 'users/index', :locals => {:users => @project.users} %>
    </div>

    <div id="process">
      <div class="swimlanes">
        <fieldset>
          <legend>Kanban Workflow (Drag and Drop to Re-order)</legend>
    		  <%= render :partial => 'card_states/index' %>
        </fieldset>
      </div>
  	  <div class="wip_enforcement_policy">
        <fieldset>
          <legend>WIP Enforcement Policy</legend>
          <input type="checkbox" name="enable_override" value="" id="enable_override" disabled>
          <label for="enable_override">Enable WIP Limits to be overridden</label>
          <br/>
          <input type="checkbox" name="warn_override" value="" id="warn_override" disabled>
          <label for="warn_override">Warn on Override</label>
        </fieldset>
      </div>
  	  <div class="wip_enforcement_policy">
        <fieldset>
          <legend>Kanban Card Display Options</legend>
          <input type="checkbox" name="prefix_title_with_card_number" value="" id="prefix_title_with_card_number" disabled>
          <label for="prefix_title_with_card_number">Prefix Title with Card #</label>
          <br/>
        </fieldset>
      </div>
	  </div>
	  

</div>

<% content_for :tail_javascript do %>
<script type="text/javascript">
    $(function() {
        $('#project_settings_tabs').tabs();
    });
</script>
<% end %>
